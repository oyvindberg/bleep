# google-java-format configuration for bleep itself.
# Vendored sbt-plugin source under liberated/** keeps its upstream style;
# we don't want bleep fmt to rewrite it.
excludePaths = ["glob:liberated/**"]
