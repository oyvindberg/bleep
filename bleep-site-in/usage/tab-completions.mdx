# Installing tab completions

Bleep has tab completions for bash. PRs welcome for other shells.

To install them run
```sh
$ bleep install-tab-completions-bash
```

Then source `~/.profile` or start a new shell to enable them
