---
title: bleep sourcegen
---

{/* AUTO-GENERATED by `bleep gen-cli-docs`. Do not edit by hand —
    edit the decline `Opts.subcommand(name, description)` calls in
    bleep-cli/src/scala/bleep/Main.scala instead. */}

# `bleep sourcegen`

<p>run source generators for projects</p>

## Synopsis

```bash
bleep sourcegen <project name (with sourcegen)...> [flags]
```

## Arguments

| Argument | Type |
|----------|------|
| `project name (with sourcegen)` | one or more |

## Flags

| Flag | Description |
|------|-------------|
| `--watch, -w` | start in watch mode |

