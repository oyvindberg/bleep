---
title: bleep history
---

import { AsciinemaPlayer } from "@site/src/components/AsciinemaPlayer";
import diffCast from "!!file-loader!@site/static/demos/diff.cast";

{/* AUTO-GENERATED by `bleep gen-cli-docs`. Do not edit by hand.
    Edit the decline `Opts.subcommand(name, description)` calls in
    bleep-cli/src/scala/bleep/Main.scala instead. */}

# `bleep history`

<p>list this workspace's recorded compile/test runs (written by the compile server, kept per worktree)</p>

## Synopsis

```bash
bleep history <subcommand> [args] [flags]
```

## Subcommands

- [`bleep history show`](./show): print the full transcript of a recorded compile/test run as JSON (latest when no id is given)
- [`bleep history diff`](./diff): diff two recorded runs: what logically changed (or, with --timing, what got slower/faster)

## Demo

An already-warm workspace: break a function, let [`bleep test --diff`](/docs/reference/cli/test/)
pinpoint the newly failing test, fix it, and compare any two runs after the fact with
[`bleep history diff`](./diff):

<AsciinemaPlayer src={diffCast} cols={100} rows={30} fit="width" />
