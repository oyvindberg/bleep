---
title: bleep setup-mcp-server
---

{/* AUTO-GENERATED by `bleep gen-cli-docs`. Do not edit by hand —
    edit the decline `Opts.subcommand(name, description)` calls in
    bleep-cli/src/scala/bleep/Main.scala instead. */}

# `bleep setup-mcp-server`

<p>generate .mcp.json so AI agents (Claude Code, etc.) can compile and test</p>

## Synopsis

```bash
bleep setup-mcp-server [flags]
```

## Flags

| Flag | Description |
|------|-------------|
| `--force-jvm` | force MCP server running through JVM |

