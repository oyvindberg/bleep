---
title: bleep diff
---

{/* AUTO-GENERATED by `bleep gen-cli-docs`. Do not edit by hand.
    Edit the decline `Opts.subcommand(name, description)` calls in
    bleep-cli/src/scala/bleep/Main.scala instead. */}

# `bleep diff`

<p>diff two recorded requests: what logically changed (or, with --timing, what got slower/faster)</p>

## Synopsis

```bash
bleep diff <base request id> <target request id> [flags]
```

## Arguments

| Argument | Type |
|----------|------|
| `base request id` | one |
| `target request id` | one |

## Flags

| Flag | Description |
|------|-------------|
| `--timing` | compare durations (jitter-suppressed) instead of logical outcome |
| `--limit <integer>` | --timing only: max entries per list (slower/faster/slowestInTarget) |
| `--base-dir <string>` | resolve the base id in another workspace's history (e.g. the worktree this one was forked from) |

