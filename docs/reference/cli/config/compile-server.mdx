---
title: bleep config compile-server
---

{/* AUTO-GENERATED by `bleep gen-cli-docs`. Do not edit by hand —
    edit the decline `Opts.subcommand(name, description)` calls in
    bleep-cli/src/scala/bleep/Main.scala instead. */}

# `bleep config compile-server`

<p>You can speed up normal usage by keeping the compile server running between invocations. This is where you control it</p>

## Synopsis

```bash
bleep config compile-server <subcommand>
```

## `bleep config compile-server auto-shutdown-disable`

<p>leave compile servers running between bleep invocations. this gets much better performance at the cost of memory</p>

## `bleep config compile-server auto-shutdown-enable`

<p>shut down the compile server between bleep invocations — slower (cold start each time), but frees memory</p>

## `bleep config compile-server stop-all`

<p>stop every shared compile server currently running</p>

## `bleep config compile-server max-memory`

<p>set max heap for compile server JVM (e.g. 4g, 2048m)</p>

### Synopsis

```bash
bleep config compile-server max-memory <size>
```

### Arguments

| Argument | Type |
|----------|------|
| `size` | one |

## `bleep config compile-server max-memory-clear`

<p>remove compile server max heap setting (use JVM default)</p>

## `bleep config compile-server heap-pressure-threshold`

<p>set heap usage fraction (0.0-1.0) above which new compilations wait for memory (default: 0.80)</p>

### Synopsis

```bash
bleep config compile-server heap-pressure-threshold <threshold>
```

### Arguments

| Argument | Type |
|----------|------|
| `threshold` | one |

## `bleep config compile-server heap-pressure-threshold-clear`

<p>remove heap pressure threshold setting (use default: 0.80)</p>

