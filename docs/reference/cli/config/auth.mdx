---
title: bleep config auth
---

{/* AUTO-GENERATED by `bleep gen-cli-docs`. Do not edit by hand —
    edit the decline `Opts.subcommand(name, description)` calls in
    bleep-cli/src/scala/bleep/Main.scala instead. */}

# `bleep config auth`

<p>configure authentication for private repositories (Artifact Registry, GitHub Packages, GitLab)</p>

## Synopsis

```bash
bleep config auth <subcommand>
```

## `bleep config auth setup`

<p>interactive setup for private repository authentication</p>

## `bleep config auth list`

<p>list configured authentications</p>

### Synopsis

```bash
bleep config auth list [flags]
```

### Flags

| Flag | Description |
|------|-------------|
| `--output, -o <string>` | output format: text, json, or raw |

## `bleep config auth remove`

<p>remove authentication for a repository URI prefix</p>

### Synopsis

```bash
bleep config auth remove <uri-prefix>
```

### Arguments

| Argument | Type |
|----------|------|
| `uri-prefix` | one |

