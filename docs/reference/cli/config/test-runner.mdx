---
title: bleep config test-runner
---

{/* AUTO-GENERATED by `bleep gen-cli-docs`. Do not edit by hand —
    edit the decline `Opts.subcommand(name, description)` calls in
    bleep-cli/src/scala/bleep/Main.scala instead. */}

# `bleep config test-runner`

<p>configure test runner JVM settings</p>

## Synopsis

```bash
bleep config test-runner <subcommand>
```

## `bleep config test-runner max-memory`

<p>set max heap for test runner JVMs (e.g. 512m, 2g)</p>

### Synopsis

```bash
bleep config test-runner max-memory <size>
```

### Arguments

| Argument | Type |
|----------|------|
| `size` | one |

## `bleep config test-runner max-memory-clear`

<p>remove test runner max heap setting (use JVM default)</p>

