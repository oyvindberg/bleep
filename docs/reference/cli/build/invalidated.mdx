---
title: bleep build invalidated
---

{/* AUTO-GENERATED by `bleep gen-cli-docs`. Do not edit by hand —
    edit the decline `Opts.subcommand(name, description)` calls in
    bleep-cli/src/scala/bleep/Main.scala instead. */}

# `bleep build invalidated`

<p>list projects whose source or config changed vs a base commit (and their transitive dependents)</p>

## Synopsis

```bash
bleep build invalidated [flags]
```

## Flags

| Flag | Description |
|------|-------------|
| `--base, -b <string>` | git commitish to compare against (e.g., origin/master) |
| `--json` | output as JSON (alias for --output json) |
| `--output, -o <string>` | output format: text, json, or raw |

