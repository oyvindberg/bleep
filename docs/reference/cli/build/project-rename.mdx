---
title: bleep build project-rename
---

{/* AUTO-GENERATED by `bleep gen-cli-docs`. Do not edit by hand —
    edit the decline `Opts.subcommand(name, description)` calls in
    bleep-cli/src/scala/bleep/Main.scala instead. */}

# `bleep build project-rename`

<p>rename a project; references in dependsOn and templates update automatically</p>

## Synopsis

```bash
bleep build project-rename <project name (not cross id)> <new project name>
```

## Arguments

| Argument | Type |
|----------|------|
| `project name (not cross id)` | one |
| `new project name` | one |

