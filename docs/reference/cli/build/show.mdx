---
title: bleep build show
---

{/* AUTO-GENERATED by `bleep gen-cli-docs`. Do not edit by hand —
    edit the decline `Opts.subcommand(name, description)` calls in
    bleep-cli/src/scala/bleep/Main.scala instead. */}

# `bleep build show`

<p>show project configuration as YAML — either as written or with templates applied</p>

## Synopsis

```bash
bleep build show <subcommand>
```

## `bleep build show short`

<p>show the project's YAML exactly as written in bleep.yaml</p>

### Synopsis

```bash
bleep build show short <project name (not cross id)...> [flags]
```

### Arguments

| Argument | Type |
|----------|------|
| `project name (not cross id)` | one or more |

### Flags

| Flag | Description |
|------|-------------|
| `--output, -o <string>` | output format: text, json, or raw |

## `bleep build show effective`

<p>show the project's final YAML after templates and inheritance are merged in</p>

### Synopsis

```bash
bleep build show effective <project name...> [flags]
```

### Arguments

| Argument | Type |
|----------|------|
| `project name` | one or more |

### Flags

| Flag | Description |
|------|-------------|
| `--output, -o <string>` | output format: text, json, or raw |

