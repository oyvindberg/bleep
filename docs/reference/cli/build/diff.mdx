---
title: bleep build diff
---

{/* AUTO-GENERATED by `bleep gen-cli-docs`. Do not edit by hand —
    edit the decline `Opts.subcommand(name, description)` calls in
    bleep-cli/src/scala/bleep/Main.scala instead. */}

# `bleep build diff`

<p>diff the effective (template-resolved) project config between git revisions</p>

## Synopsis

```bash
bleep build diff <subcommand>
```

## `bleep build diff effective`

<p>diff the effective (template-resolved) project config against git HEAD or another revision</p>

### Synopsis

```bash
bleep build diff effective <project name...> [flags]
```

### Arguments

| Argument | Type |
|----------|------|
| `project name` | one or more |

### Flags

| Flag | Description |
|------|-------------|
| `--revision, -r <string>` | git revision to diff build against |
| `--output, -o <string>` | output format: text, json, or raw |

