---
title: bleep build project-merge-into
---

{/* AUTO-GENERATED by `bleep gen-cli-docs`. Do not edit by hand —
    edit the decline `Opts.subcommand(name, description)` calls in
    bleep-cli/src/scala/bleep/Main.scala instead. */}

# `bleep build project-merge-into`

<p>merge the first project's sources, dependencies, and config into the second; the first project is deleted</p>

## Synopsis

```bash
bleep build project-merge-into <project name (not cross id)> <project name (not cross id)>
```

## Arguments

| Argument | Type |
|----------|------|
| `project name (not cross id)` | one |
| `project name (not cross id)` | one |

