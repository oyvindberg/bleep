---
title: bleep build projects-move
---

{/* AUTO-GENERATED by `bleep gen-cli-docs`. Do not edit by hand —
    edit the decline `Opts.subcommand(name, description)` calls in
    bleep-cli/src/scala/bleep/Main.scala instead. */}

# `bleep build projects-move`

<p>move one or more projects into a different parent folder on disk</p>

## Synopsis

```bash
bleep build projects-move <new parent folder> <project name (not cross id)...>
```

## Arguments

| Argument | Type |
|----------|------|
| `new parent folder` | one |
| `project name (not cross id)` | one or more |

