---
title: bleep build new
---

{/* AUTO-GENERATED by `bleep gen-cli-docs`. Do not edit by hand —
    edit the decline `Opts.subcommand(name, description)` calls in
    bleep-cli/src/scala/bleep/Main.scala instead. */}

# `bleep build new`

<p>scaffold a new bleep workspace in the current directory: bleep.yaml, a project, a sibling test project, hello-world source</p>

## Synopsis

```bash
bleep build new <wanted project name> [flags]
```

## Arguments

| Argument | Type |
|----------|------|
| `wanted project name` | one |

## Flags

| Flag | Description |
|------|-------------|
| `--lang, -l <string>` | specify language: java, kotlin, or scala (default: scala) |
| `--platform, -p <platform name>` (repeatable) | specify wanted platform(s) (Scala only) |
| `--scala, -s <scala version>` (repeatable) | specify scala version(s) (Scala only) |

