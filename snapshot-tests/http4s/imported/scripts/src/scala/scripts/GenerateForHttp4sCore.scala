
package scripts

import bleep.{BleepCodegenScript, Commands, Started}

import java.nio.file.Files

object GenerateForHttp4sCore extends BleepCodegenScript("GenerateForHttp4sCore") {
  override def run(started: Started, commands: Commands, targets: List[Target], args: List[String]): Unit = {
    started.logger.error("This script is a placeholder! You'll need to replace the contents with code which actually generates the files you want")

    targets.foreach { target =>
      if (Set("http4s-core@js213", "http4s-core@jvm213", "http4s-core@native213").contains(target.project.value)) {
        val to = target.sources.resolve("sbt-buildinfo/BuildInfo.scala")
        started.logger.withContext(target.project).warn(s"Writing $to")
        val content = s"""|// $$COVERAGE-OFF$$
      |package org.http4s
      |
      |/** This object was generated by sbt-buildinfo. */
      |case object BuildInfo {
      |  /** The value is "1.0-6e6ba2c-20250621T223255Z-SNAPSHOT". */
      |  val version: String = "1.0-6e6ba2c-20250621T223255Z-SNAPSHOT"
      |  /** The value is "2.13.15". */
      |  val scalaVersion: String = "2.13.15"
      |  /** The value is (1 -> 0). */
      |  val apiVersion = (1 -> 0)
      |  override val toString: String = {
      |    "version: %s, scalaVersion: %s, apiVersion: %s".format(
      |      version, scalaVersion, apiVersion
      |    )
      |  }
      |}
      |// $$COVERAGE-ON$$""".stripMargin
        Files.createDirectories(to.getParent)
        Files.writeString(to, content)
      }
    }



    targets.foreach { target =>
      if (Set("http4s-core@js3", "http4s-core@jvm3", "http4s-core@native3").contains(target.project.value)) {
        val to = target.sources.resolve("sbt-buildinfo/BuildInfo.scala")
        started.logger.withContext(target.project).warn(s"Writing $to")
        val content = s"""|// $$COVERAGE-OFF$$
      |package org.http4s
      |
      |/** This object was generated by sbt-buildinfo. */
      |case object BuildInfo {
      |  /** The value is "1.0-6e6ba2c-20250621T223354Z-SNAPSHOT". */
      |  val version: String = "1.0-6e6ba2c-20250621T223354Z-SNAPSHOT"
      |  /** The value is "3.3.4". */
      |  val scalaVersion: String = "3.3.4"
      |  /** The value is (1 -> 0). */
      |  val apiVersion = (1 -> 0)
      |  override val toString: String = {
      |    "version: %s, scalaVersion: %s, apiVersion: %s".format(
      |      version, scalaVersion, apiVersion
      |    )
      |  }
      |}
      |// $$COVERAGE-ON$$""".stripMargin
        Files.createDirectories(to.getParent)
        Files.writeString(to, content)
      }
    }

  }
}